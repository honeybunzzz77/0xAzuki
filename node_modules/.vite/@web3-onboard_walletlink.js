import {
  init_buffer,
  init_virtual_process_polyfill
} from "./chunk-QYANIK43.js";

// dep:@web3-onboard_walletlink
init_virtual_process_polyfill();
init_buffer();

// node_modules/@web3-onboard/walletlink/dist/index.js
init_virtual_process_polyfill();
init_buffer();
function walletLink(options) {
  const { darkMode = false } = options || {};
  return () => {
    return {
      label: "Coinbase",
      getIcon: async () => (await import("./icon-3UL3NCJ3.js")).default,
      getInterface: async ({ chains, appMetadata }) => {
        const [chain] = chains;
        const { name, icon } = appMetadata || {};
        const { WalletLink } = await import("./dist-LP55SUQE.js");
        const base64 = window.btoa(icon || "");
        const appLogoUrl = `data:image/svg+xml;base64,${base64}`;
        const instance = new WalletLink({
          appName: name || "",
          appLogoUrl,
          darkMode
        });
        const walletLinkProvider = instance.makeWeb3Provider(chain.rpcUrl, parseInt(chain.id));
        const on = walletLinkProvider.on.bind(walletLinkProvider);
        walletLinkProvider.on = (event, listener) => {
          on(event, (val) => {
            if (event === "chainChanged") {
              listener(`0x${val.toString(16)}`);
              return;
            }
            listener(val);
          });
          return walletLinkProvider;
        };
        return {
          provider: walletLinkProvider,
          instance
        };
      }
    };
  };
}
var dist_default = walletLink;

// dep:@web3-onboard_walletlink
var web3_onboard_walletlink_default = dist_default;
export {
  web3_onboard_walletlink_default as default
};
//# sourceMappingURL=@web3-onboard_walletlink.js.map
